# Build artifacts
*.zip

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Local server logs
*.log
.cache-catalogue.json
.cache-catalogue.tsv
.cache-seo.txt
.cache-wp.txt
.cache-docs/
.cache-training.xlsx

# Image-tools + prerender: commit the source + manifests, ignore installed deps.
# node_modules holds sharp + puppeteer's ~170 MB Chromium download.
_imgtools/node_modules/
_imgtools/.cache/

# Puppeteer's global Chromium cache (paths vary by OS)
.cache/puppeteer/
